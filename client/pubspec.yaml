name: remote_shutdown_app
description: "A simple Flutter app to remotely shut down a PC."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  cupertino_icons: ^1.0.8
  flutter_animate: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.2
  build_runner: ^2.4.6  # Pour générer les mocks

flutter:
  uses-material-design: true
  assets:
    - assets/shutdown_icon.png